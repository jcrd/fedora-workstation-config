#!/bin/sh

readonly STEP=${BRIGHTNESS_STEP:-10}

command -v ddcutil > /dev/null || exit 1

update() {
    ddcutil setvcp 10 $1 ${2:-$STEP}
}

case "$1" in
    inc) update + $2 ;;
    dec) update - $2 ;;
    *) echo 'usage: brightness inc|dec [STEP]' >&2; exit 2
esac
